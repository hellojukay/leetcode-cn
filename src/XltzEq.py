class Solution:
    def isPalindrome(self, s: str) -> bool:
        import re
        result = ""
        for c in s:
            if str(c).isalnum()
            result = result + str(c)
        return result.lower() == result.lower()[::-1]
